{
  "buildCommand": "cd timetable && npm install && npm run build",
  "outputDirectory": "timetable/build",
  "installCommand": "npm install --prefix timetable",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}